# Generated by setup.sh at runtime
.playwright/

# Agent config: the playwright-cli skill is regenerated by setup.sh and
# settings.local.json is personal. Scoped rather than ignoring .claude/
# wholesale, so project-level config stays committable.
.claude/skills/playwright-cli/
.claude/settings.local.json

# Local environment overrides
.env
.env.*
